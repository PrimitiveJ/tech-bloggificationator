<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date date_created}}
    </div>
    <div class="text">
      {{commentary}}
    </div>
  </section>
  {{/each}}
</div>